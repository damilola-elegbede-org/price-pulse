# Build better-sqlite3 from source rather than downloading a prebuilt binary.
# Eliminates the prebuild-install supply-chain risk (deprecated; binary integrity
# not covered by lockfile hashes). Requires node-gyp + Python + C++ toolchain.
better_sqlite3_build_from_source=true
